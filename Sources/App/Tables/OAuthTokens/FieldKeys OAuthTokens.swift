
extension OAuthTokens {
    enum FieldKeys: String {
        case accessToken
        case refreshToken
        case expiresIn
        case scope
        case tokenType
        case createdAt
        case issuer
        case streamerId
    }
}
